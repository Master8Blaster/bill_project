import 'package:flutter/material.dart';

bool isTest = true;

const double borderRadius = 8;
const double spaceHorizontal = 10;
const double spaceVertical = 10;


const Duration switcherDuration = Duration(milliseconds: 400);

BorderRadius boxBorderRadius = BorderRadius.circular(8);
